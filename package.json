{
  "name": "methylscape-analysis",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write --ignore-unknown '**/*.{js,jsx,ts,tsx,json,css,scss,md}'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.3.7",
    "prettier": "^2.6.1"
  },
  "prettier": {
    "printWidth": 120,
    "quoteProps": "consistent",
    "bracketSameLine": true
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
